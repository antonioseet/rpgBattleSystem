using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Person
{
    // Start is called before the first frame update
    public bool hasCancer = false;

    public Person()
    {
        
    }

    // Update is called once per frame
    public bool wasCured()
    {
        return true;
    }
}
